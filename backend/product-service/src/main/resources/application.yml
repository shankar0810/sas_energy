server:
  port: 3334
spring:

  datasource:
    url: jdbc:mysql://localhost:3306/sas_energy
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
